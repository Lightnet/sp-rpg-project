class ScriptBehavior extends Sup.Behavior {
  awake() {
     //var socketio = io('ws://localhost');
      //socketio.on('connect',()=>{
//        Sup.log('connected');      
//      });
  }

  update() {
    
  }
}
Sup.registerBehavior(ScriptBehavior);
